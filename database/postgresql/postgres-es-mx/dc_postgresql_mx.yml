services:
  bd_demo:
    image: postgres-mx:18.4
    volumes:
      - postgresql_mx_data:/var/lib/postgresql
    restart: always
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_INITDB_ARGS: '--encoding=UTF-8 --lc-collate=es_MX.utf8 --lc-ctype=es_MX.utf8'  # Comenta y descomenta estas lineas
      LANG: es_MX.utf8                                                                        # alternalas para ver los diferentes
      TZ: ${TZ}                                                                               # comportamientos
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"
volumes:
  postgresql_mx_data: {}
